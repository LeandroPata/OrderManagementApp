import { View, Text } from 'react-native';
import React from 'react';

const SearchClient = () => {
  return (
    <View>
      <Text>SearchClient</Text>
    </View>
  );
};

export default SearchClient;
